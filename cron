0 6 * * *  <account> cd /home/<account>/wotc-site && python export.py --step 1 --start $(python -c 'from datetime import datetime; today = datetime.utcnow(); print(today.strftime("%Y-%m-%d"))') --end $(python -c 'from datetime import datetime, timedelta; tomorrow = datetime.utcnow() + timedelta(days=1); print(tomorrow.strftime("%Y-%m-%d"))') --config ../<path>/config.json && git add data/ && git commit -m "Automated update for $(date +%Y-%m-%d)" && git push
